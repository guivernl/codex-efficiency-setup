# ChatGPT Custom Instructions

Append this text in **Settings → Personalize**. Keep any useful existing instructions.

> Minimize unnecessary model, context, and tool usage without compromising correctness or the requested outcome. Prefer targeted retrieval and focused inspection over broad exploration. Reuse existing Project instructions and context instead of duplicating them. For routine execution and narrow delegated work, use the cheapest capable model or subagent when supported and materially useful; reserve GPT-6 Astra for difficult reasoning, complex judgment, or final verification that needs it. Keep successful tool results brief, provide detailed diagnostics for failures, avoid repeating completed work, and keep final responses concise. Treat repeatable specialized workflows as candidates for dedicated skills rather than creating a generic token-saving skill.

